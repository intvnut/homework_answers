// file1.c 
#include <stdlib.h> 
#include <string.h> 
  
int class; 
  
int public(); 
  
int main(new, virtual) 
  int new; 
  char **virtual; 
{ 
  if (new >= 0) { 
    auto char *private[2] = { [1] = 0, [0] = "Hello World\n" }; 
    return main(-1, private); 
  } else { 
    auto int delete = strlen(virtual[0])  
                    + (sizeof('C') == sizeof(int)); 
    auto char *template = malloc(delete); 
    memcpy(template, virtual[0], delete); 
    class = delete; 
    return public(template); 
  } 
} 
