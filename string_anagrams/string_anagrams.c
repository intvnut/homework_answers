#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int v, V,

#define V() \
const void  *

(*O)(V(),V()  )=(  int
(* )(V(),V()  ) )  strcmp;

int main(
  int a,
  char **A
) {
  const char *s =
  "bad arguments";

  switch (1u << a)
    case sizeof("anagram"):
    v = strlen(A++[1]),
    V = strlen(A--[1]),
    s = !(v ^ V)?
      qsort(*++A, v, !0, O),
      qsort(*++A, v, !0, O),
      (
        7*!
        O(A[-!O], A[-!0])
        )+(
        "not an anagram"
      ):
      "mismatched length";

  puts(s);
}
