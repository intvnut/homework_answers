#include <iostream>

int main() {
  auto C{std::getchar};
  auto&_{std::cout};
  auto s{0x21},t{0},u{0},d{0},y{0};
  auto h{0},a{0},r{0},D{30},e{0},R{0};

  for (
  auto c{0};
  c >= 0 & e < D;
  c  = C())
  e +=
  R  = y > (
  u  = ((
  c -= '0') < 0xAu)),
  a += R * d & !r,
  t += R * s & h,
  d &= ~R,
  d ^=
  c == '-'-'0',
  r  = !u | (r & !(
  h  = c)),
  h &= !!(
  y  = u),
  h -= r + r;
  s  = (1 - s) & t ^ t;
  t  = (t ^ s) % 037;
  u  = e - t - (
  d  = a);
  y  = e - s;
  _ <<
  s << ' ' <<
  t << ' ' <<
  u << ' ' <<
  d << ' ' <<
  y << '\n';
}
